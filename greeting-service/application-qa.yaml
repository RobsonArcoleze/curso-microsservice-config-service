#server:
#  port: ${PORT:8080}

spring:
  application:
    name: greeting-service

greeting-service:
  greeting: "Olá"
  default-value: "Mundo"

management:
  endpoints:
    web:
      exposure:
        include:
          - '*'






